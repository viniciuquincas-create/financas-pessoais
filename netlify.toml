[build]
  base = "finance-app"
  command = "npm run build"
  publish = "finance-app/dist"

[functions]
  directory = "netlify/functions"

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
